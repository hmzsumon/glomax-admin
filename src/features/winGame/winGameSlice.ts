import { createSlice } from '@reduxjs/toolkit';

const initialState = {};

export const winGameSlice = createSlice({
	name: 'winGame',
	initialState,
	reducers: {},
});

export const {} = winGameSlice.actions;
export default winGameSlice.reducer;
